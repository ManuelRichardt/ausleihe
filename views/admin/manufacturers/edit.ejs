<% pageTitle = 'Hersteller bearbeiten' %>
<%- include('./form', {
  heading: 'Hersteller bearbeiten',
  subtitle: (manufacturer && manufacturer.name) ? manufacturer.name : '',
  backHref: (manufacturer && manufacturer.id) ? `/admin/manufacturers/${manufacturer.id}` : '/admin/manufacturers',
  backLabel: 'Zurück',
  formAction: (manufacturer && manufacturer.id) ? `/admin/manufacturers/${manufacturer.id}` : '/admin/manufacturers',
  submitLabel: 'Speichern',
  cancelHref: (manufacturer && manufacturer.id) ? `/admin/manufacturers/${manufacturer.id}` : '/admin/manufacturers',
  csrfToken,
  formData,
  errors,
  entity: manufacturer,
}) %>
