<% pageTitle = 'Kategorie bearbeiten' %>
<%- include('./form', {
  heading: 'Kategorie bearbeiten',
  subtitle: (category && category.name) ? category.name : '',
  backHref: (category && category.id) ? `/admin/categories/${category.id}` : '/admin/categories',
  backLabel: 'Zurück',
  formAction: (category && category.id) ? `/admin/categories/${category.id}` : '/admin/categories',
  submitLabel: 'Speichern',
  cancelHref: (category && category.id) ? `/admin/categories/${category.id}` : '/admin/categories',
  csrfToken,
  formData,
  errors,
  entity: category,
}) %>
