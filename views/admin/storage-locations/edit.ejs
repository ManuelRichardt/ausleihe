<% pageTitle = 'Lagerort bearbeiten' %>
<%- include('./form', {
  heading: 'Lagerort bearbeiten',
  subtitle: (storageLocation && storageLocation.name) ? storageLocation.name : '',
  backHref: (storageLocation && storageLocation.id) ? `/admin/storage-locations/${storageLocation.id}` : '/admin/storage-locations',
  backLabel: 'Zurück',
  formAction: (storageLocation && storageLocation.id) ? `/admin/storage-locations/${storageLocation.id}` : '/admin/storage-locations',
  submitLabel: 'Speichern',
  cancelHref: (storageLocation && storageLocation.id) ? `/admin/storage-locations/${storageLocation.id}` : '/admin/storage-locations',
  csrfToken,
  formData,
  errors,
  entity: storageLocation,
}) %>
