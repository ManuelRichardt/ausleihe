<% pageTitle = 'Asset Models' %>
<%- include('../partials/listLayout', {
  sectionClass: 'models',
  heading: 'Asset Models',
  filters: filters,
  pagination: pagination,
  items: models,
  filterAction: '/admin/asset-models',
  filterPartial: '../models/indexFilters',
  createHref: '/admin/asset-models/new',
  createLabel: 'Neues Model',
  manufacturers: manufacturers,
  categories: categories,
  columns: [
    { label: 'Name', valuePath: 'name', sortableField: 'name' },
    { label: 'Hersteller', valuePath: 'manufacturer.name', fallback: '-' },
    { label: 'Kategorie', valuePath: 'category.name', fallback: '-' },
  ],
  actions: {
    basePath: '/admin/asset-models',
  },
  emptyMessage: 'Keine Modelle vorhanden.',
  csrfToken: csrfToken,
}) %>
