<% pageTitle = 'Asset Model bearbeiten' %>
<%- include('./form', {
  mode: 'edit',
  heading: 'Asset Model bearbeiten',
  subtitle: (model && model.name) ? model.name : '',
  backHref: (model && model.id) ? `/admin/asset-models/${model.id}` : '/admin/asset-models',
  backLabel: 'Zurück',
  formAction: (model && model.id) ? `/admin/asset-models/${model.id}` : '/admin/asset-models',
  submitLabel: 'Speichern',
  cancelHref: (model && model.id) ? `/admin/asset-models/${model.id}` : '/admin/asset-models',
  csrfToken,
  formData,
  errors,
  model,
  manufacturers,
  categories,
  customFieldDefinitions,
  componentModels,
  bundleDefinition,
  stock,
  lendingLocation,
  lendingLocationId,
}) %>
<link rel="stylesheet" href="/vendor/quill/quill.snow.css">
<script src="/vendor/quill/quill.js"></script>
<script src="/js/assetModelForm.js?v=20260522-quill"></script>
